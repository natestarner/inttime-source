spring:
  datasource:
    url: jdbc:sqlserver://localhost:1433;database=inttime;encrypt=false;trustServerCertificate=true
    username: sa
    password: LocalDev123!
  jpa:
    hibernate:
      ddl-auto: validate    # Flyway manages schema
    show-sql: true
  flyway:
    enabled: true
    baseline-on-migrate: true
    clean-disabled: false    # OK to allow clean locally (never in prod)